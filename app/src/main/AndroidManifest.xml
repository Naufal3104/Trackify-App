<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.trackergps">

    <!-- Permissions -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.TrackerGPS"
        tools:targetApi="31">

        <!-- Splash screen -->
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Login & Register -->
        <activity android:name=".LoginActivity" android:exported="false" />
        <activity android:name=".RegisterActivity" android:exported="false" />

        <!-- Admin Frame (host fragment) -->
        <activity android:name=".AdminFrame" android:exported="false" />

        <!-- User Frame (host fragment) -->
        <activity android:name=".user.UserFrame" android:exported="false" />

        <!-- Jika masih digunakan, tambahkan activity lainnya -->
        <!-- <activity android:name=".dashboard_user" android:exported="false" /> -->
        <!-- <activity android:name=".ProfileAdmin" android:exported="false" /> -->
        <!-- <activity android:name=".ManageAdmin" android:exported="false" /> -->
        <!-- <activity android:name=".UserActivity" android:exported="false" /> -->

        <!-- ✅ Service: LocationService -->
        <service
            android:name=".LocationService"
            android:enabled="true"
            android:exported="false"
            android:foregroundServiceType="location"
            tools:ignore="ForegroundServicePermission" />

    </application>
</manifest>
